class Solution {
    public int maxProfit(int[] prices) {
        int min = prices[0], maxProfit = 0;
        for(int i : prices){
            min = Math.min(i, min);
            maxProfit = Math.max(maxProfit, i - min);
        }
        return maxProfit;
    }
}
