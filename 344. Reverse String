class Solution {
    public void reverseString(char[] s) {
        int n = s.length;
        char tmp;
        for (int i = 0; i < n / 2; i++){
            tmp = s[n - 1 - i];
            s[n -i - 1] = s[i];
            s[i] = tmp;
        }
    }
}
